fastapi==0.121.3
uvicorn[standard]==0.38.0
pydantic==2.12.4
python-dotenv>=1.0,<2.0
langchain==1.0.8
langgraph==1.0.3
langchain-community==0.4.1
langchain-openai==1.0.3
langchain-ollama==1.0.1  # 仅用于ChatOllama/本地LLM；embedding仍建议自定义实现
# NOTE: 报错是因为 PyPI 上并不存在 0.4.x 这个版本区间；可用版本是 0.3.x 或 1.0.x，所以必须改成 0.3.* 或 1.0.*。

langchain-chroma==1.0.0
chromadb==1.3.5
redis==7.1.0

tiktoken==0.12.0
pypdf==6.4.1
python-docx==1.2.0
rank-bm25==0.2.2
langchain-text-splitters~=1.0.0
ollama~=0.6.1

# NOTE: chromadb/chromadb-client 系列通常要求 bcrypt>=4；如果你用的是 Python 3.13 且 bcrypt 没有可用 wheel，优先把虚拟环境降到 Python 3.11/3.12 再装。
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.0.0